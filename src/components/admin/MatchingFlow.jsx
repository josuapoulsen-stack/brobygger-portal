/**
 * src/components/admin/MatchingFlow.jsx
 *
 * FASE 2: 3-trins matching-wizard.
 * Vælg menneske → vælg brobygger (suggestioner fra Matching.getSuggestions) → bekræft.
 */

export default function MatchingFlow({ onComplete, onCancel }) {
  return null;
}
