/**
 * src/components/brobygger/HomeScreen.jsx
 *
 * FASE 2: Migrer HomeScreen (brobygger-forside med aftaler + notifikationer).
 *
 * Props:
 *   user         — { id, navn, rolle }
 *   onNavigate   — (screen) => void
 */

export default function HomeScreen({ user, onNavigate }) {
  return null;
}
