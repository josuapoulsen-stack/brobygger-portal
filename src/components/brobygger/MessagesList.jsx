/**
 * src/components/brobygger/MessagesList.jsx
 *
 * FASE 2: Beskeder + notifikationer (to-tab komponent).
 * Realtime via SignalR-hook (useSignalR).
 *
 * Props:
 *   user         — { id, navn, rolle }
 *   onNavigate   — (screen) => void
 */

export default function MessagesList({ user, onNavigate }) {
  return null;
}
