/**
 * src/components/brobygger/ProfileScreen.jsx
 *
 * FASE 2: Profil med undersider (personlige oplysninger, præferencer, privatliv).
 * Henter og gemmer data via Profil.get() / Profil.update().
 */

export default function ProfileScreen({ user }) {
  return null;
}
