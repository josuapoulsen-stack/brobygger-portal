/**
 * src/components/brobygger/KalenderScreen.jsx
 *
 * FASE 2: Migrer KalenderScreen fra "Brobygger portal.html" hertil.
 *
 * Props:
 *   user         — { id, navn, rolle }
 *   onNavigate   — (screen) => void
 *
 * Data:
 *   Prototype: window.SoS_APPOINTMENTS_BUSY
 *   Produktion: Aftaler.getAll({ brobyggerId: user.id })
 */

// TODO: Migrer komponent fra HTML-prototypen
export default function KalenderScreen({ user, onNavigate }) {
  return null; // placeholder — erstat med migreret komponent
}
